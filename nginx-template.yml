- name: Deploy nginx config
  hosts: servers
  become: yes

  vars:
    port: 80
    domain: myapp.com

  tasks:
    - name: Copy nginx config
      template:
        src: nginx.conf.j2
        dest: /etc/nginx/sites-available/default
